{
  "name": "websemantique",
  "version": "0.0.1",
  "description": "A search engine implementation in NodeJS",
  "main": "index.js",
  "scripts": {
    "compile": "babel --experimental -d dist/ src/",
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run compile && node ./dist/main.js"
  },
  "author": "",
  "license": "ISC"
}
